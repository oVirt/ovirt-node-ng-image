---
stage:
  - check-merged
  - build-artifacts
  - check-patch:
      runtime-requirements:
        support-nesting-level: 2
distro:
  - el8
  - el9stream:
      runtime-requirements:
        host_distro: newer
release_branches:
  master: ovirt-master
